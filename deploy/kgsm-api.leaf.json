{
  "schemaVersion": 1,
  "id": "api",
  "displayName": "Control Panel API",
  "unit": "kgsm-api.service",
  "role": "The aggregator serving this Control Panel — the API every surface on this host talks to.",
  "onDemand": false,
  "applyMode": "restart",
  "readOnly": true,
  "readOnlyReason": "The Control Panel API publishes its own configuration for reading only. Applying a change means restarting it, which would kill the request asking for the change and take the panel down with it — so its settings are edited in /etc/kgsm-api/kgsm-api.env and applied by restarting the service.",
  "floorSources": [
    {
      "kind": "appsettings",
      "path": "/opt/kgsm-api/kgsm-api.settings.json"
    },
    {
      "kind": "systemd-unit",
      "path": "kgsm-api.service"
    },
    {
      "kind": "env-file",
      "path": "/etc/kgsm-api/kgsm-api.env"
    }
  ],
  "groups": [
    {
      "id": "identity",
      "label": "Identity",
      "order": 1
    },
    {
      "id": "leaves",
      "label": "Leaf connections",
      "order": 2
    },
    {
      "id": "engine",
      "label": "KGSM engine",
      "order": 3
    },
    {
      "id": "polling",
      "label": "Polling",
      "order": 4
    },
    {
      "id": "auth",
      "label": "Authentication",
      "order": 5
    },
    {
      "id": "sessions",
      "label": "Sessions",
      "order": 6
    },
    {
      "id": "library",
      "label": "Game library & cover art",
      "order": 7
    },
    {
      "id": "files",
      "label": "File & blueprint editing",
      "order": 8
    },
    {
      "id": "logs",
      "label": "Log reading",
      "order": 9
    },
    {
      "id": "leafconfig",
      "label": "Leaf configuration",
      "order": 10
    },
    {
      "id": "cluster",
      "label": "Cluster",
      "order": 11
    },
    {
      "id": "storage",
      "label": "Storage",
      "order": 12
    },
    {
      "id": "general",
      "label": "General",
      "order": 13
    }
  ],
  "fields": [
    {
      "key": "hostId",
      "env": "Api__HostId",
      "label": "Host id",
      "description": "Identity this host is known by across the whole ecosystem — the audience its tokens are minted for, and the key its stored metrics and history hang off. Empty uses the machine name.",
      "group": "identity",
      "type": "string",
      "risk": "wiring"
    },
    {
      "key": "hostLabel",
      "env": "Api__HostLabel",
      "label": "Display name",
      "description": "Friendly name shown for this host in the panel. Empty falls back to the host id.",
      "group": "identity",
      "type": "string",
      "risk": "safe"
    },
    {
      "key": "region",
      "env": "Api__Region",
      "label": "Region",
      "description": "Where this host is, shown alongside it in a multi-host view. Free text, purely descriptive.",
      "group": "identity",
      "type": "string",
      "risk": "safe"
    },
    {
      "key": "nodeId",
      "env": "Api__NodeId",
      "label": "Cluster node id",
      "description": "Identity this host uses inside a cluster. Empty uses the host id.",
      "group": "identity",
      "type": "string",
      "risk": "wiring"
    },
    {
      "key": "publicBaseUrl",
      "env": "Api__PublicBaseUrl",
      "label": "Public base URL",
      "description": "Externally reachable address of this node, used where a link has to work from outside the host. Leave empty in a cluster with a DNS anchor, which names the node; links are then built from the incoming request.",
      "group": "identity",
      "type": "string",
      "risk": "safe"
    },
    {
      "key": "connectHost",
      "env": "Api__ConnectHost",
      "label": "Player connect host",
      "description": "The address players type to join servers on this host, without a port. Set it when players reach this host at a different address than the panel does. Empty means surfaces use the address they reached this host at.",
      "group": "identity",
      "type": "string",
      "risk": "safe"
    },
    {
      "key": "publicHost",
      "env": "Api__PublicHost",
      "label": "Public host",
      "description": "Where this host is reached from the internet — normally the dynamic-DNS name its network keeps pointed at a changing home address, such as example.ddns.net. In a cluster with a DNS anchor, this node's name is published as an alias of it, so an address change moves nothing in the cluster. A fixed public address works too. Empty means this node is named and not published.",
      "group": "identity",
      "type": "string",
      "risk": "safe"
    },
    {
      "key": "monitorSocket",
      "env": "Api__MonitorSocketPath",
      "label": "Monitor socket",
      "description": "The metrics daemon's socket. Wrong and the metrics capability reports unavailable rather than guessing at numbers.",
      "group": "leaves",
      "type": "path",
      "default": "/run/kgsm-monitor/metrics.sock",
      "risk": "wiring"
    },
    {
      "key": "watchdogSocket",
      "env": "Api__WatchdogSocketPath",
      "label": "Watchdog socket",
      "description": "The supervisor's control socket, which every start and stop of a native server goes through.",
      "group": "leaves",
      "type": "path",
      "default": "/run/kgsm-watchdog/control.sock",
      "risk": "wiring"
    },
    {
      "key": "assistantUrl",
      "env": "Api__AssistantBaseUrl",
      "label": "Assistant URL",
      "description": "Where the assistant serves. Empty means this host has no assistant and the chat surface reports it absent.",
      "group": "leaves",
      "type": "string",
      "risk": "wiring"
    },
    {
      "key": "assistantPublicUrl",
      "env": "Api__AssistantPublicUrl",
      "label": "Assistant public URL",
      "description": "The assistant's own public address, e.g. https://assistant.example.com. The Control Panel's chat talks to the assistant directly on it. Empty means the chat reports the assistant unreachable from the browser rather than routing through this API.",
      "group": "leaves",
      "type": "string",
      "risk": "wiring"
    },
    {
      "key": "firewallSocket",
      "env": "Api__FirewallSocketPath",
      "label": "Firewall socket",
      "description": "The firewall authority's socket. Empty means port state is reported as unknown rather than assumed closed.",
      "group": "leaves",
      "type": "path",
      "risk": "wiring"
    },
    {
      "key": "schedulerSocket",
      "env": "Api__SchedulerSocketPath",
      "label": "Scheduler socket",
      "description": "The scheduler's status socket. Empty means the next scheduled restart shows as unknown.",
      "group": "leaves",
      "type": "path",
      "risk": "wiring"
    },
    {
      "key": "schedulerControlSocket",
      "env": "Api__SchedulerControlSocketPath",
      "label": "Scheduler control socket",
      "description": "The scheduler's control socket, which postponing a scheduled restart goes through. Empty means restarts can be seen but not deferred.",
      "group": "leaves",
      "type": "path",
      "risk": "wiring"
    },
    {
      "key": "reactorSocket",
      "env": "Api__ReactorSocketPath",
      "label": "Reactor socket",
      "description": "The reactor's status socket, which its health is read from. Empty means the Services page shows systemd liveness only.",
      "group": "leaves",
      "type": "path",
      "risk": "wiring"
    },
    {
      "key": "speechSocket",
      "env": "Api__SpeechSocketPath",
      "label": "Speech socket",
      "description": "The speech engine's socket, which the Services page reads its engine state from. The socket is bound whether or not the daemon is running, so a host without kgsm-speech simply has no file there and the page says so.",
      "group": "leaves",
      "type": "path",
      "default": "/run/kgsm-speech/speech.sock",
      "risk": "wiring"
    },
    {
      "key": "botSocket",
      "env": "Api__BotSocketPath",
      "label": "Discord bot socket",
      "description": "The Discord bot's status socket, which the Services page reads its gateway and channel state from. Empty means the bot page shows systemd liveness only.",
      "group": "leaves",
      "type": "path",
      "risk": "wiring"
    },
    {
      "key": "kgsmPath",
      "env": "Api__KgsmPath",
      "label": "KGSM executable",
      "description": "Path to the KGSM executable. Everything this API knows about servers, blueprints and configuration is read through it.",
      "group": "engine",
      "type": "path",
      "default": "/usr/bin/kgsm",
      "risk": "wiring"
    },
    {
      "key": "kgsmJournalDir",
      "env": "Api__KgsmJournalDir",
      "label": "KGSM event journal",
      "description": "Directory holding the engine's append-only event journal, which the API reads to see what the engine did. Read-only and shared with every other consumer — nothing needs configuring on the engine side.",
      "group": "engine",
      "type": "path",
      "default": "/var/lib/kgsm/events",
      "risk": "wiring"
    },
    {
      "key": "eventJournalDir",
      "env": "Api__EventJournalDir",
      "label": "Panel event journal",
      "description": "Where this API records what it did itself — signing people in, changing an account's authority, editing a file. Its own journal, not the engine's: every component on this host records its own actions, and the audit page is the merge of all of them. Moving it leaves the existing history behind at the old path.",
      "group": "engine",
      "type": "path",
      "risk": "destructive"
    },
    {
      "key": "journalStateRoot",
      "env": "Api__JournalStateRoot",
      "label": "Service state root",
      "description": "The directory holding each KGSM service's own state directory. The audit page is the merge of every event journal found under it, so pointing it somewhere else changes which components' history this host can show.",
      "group": "engine",
      "type": "path",
      "default": "/var/lib",
      "risk": "wiring"
    },
    {
      "key": "systemctlPath",
      "env": "Api__SystemctlPath",
      "label": "systemctl path",
      "description": "Which systemctl to run when reading a leaf's state or restarting it.",
      "group": "engine",
      "type": "path",
      "default": "systemctl",
      "risk": "wiring"
    },
    {
      "key": "journalctlPath",
      "env": "Api__JournalctlPath",
      "label": "journalctl path",
      "description": "Which journalctl to run when reading a leaf's logs.",
      "group": "engine",
      "type": "path",
      "default": "journalctl",
      "risk": "wiring"
    },
    {
      "key": "domainPollMs",
      "env": "Api__DomainPollMs",
      "label": "Server list refresh",
      "description": "How often the list of servers and their run state is re-read from the engine.",
      "group": "polling",
      "type": "int",
      "default": "5000",
      "min": 1000,
      "unit": "ms",
      "risk": "safe"
    },
    {
      "key": "metricsPollMs",
      "env": "Api__MetricsPollMs",
      "label": "Metrics refresh",
      "description": "How often metrics are scraped from the monitor and pushed to connected panels.",
      "group": "polling",
      "type": "int",
      "default": "1000",
      "min": 250,
      "unit": "ms",
      "risk": "safe"
    },
    {
      "key": "servicesPollMs",
      "env": "Api__ServicesPollMs",
      "label": "Services refresh",
      "description": "How often each leaf's systemd state and health are re-read for the Services board.",
      "group": "polling",
      "type": "int",
      "default": "5000",
      "min": 2000,
      "unit": "ms",
      "risk": "safe"
    },
    {
      "key": "backupScanPollMs",
      "env": "Api__BackupScanPollMs",
      "label": "Backup scan interval",
      "description": "How often each server's backups are re-scanned for the panel's backup KPIs. A backup taken or restored through KGSM refreshes that server immediately, so this only bounds how quickly other changes are noticed.",
      "group": "polling",
      "type": "int",
      "default": "300000",
      "min": 30000,
      "unit": "ms",
      "risk": "safe"
    },
    {
      "key": "instanceCacheTtlSec",
      "env": "Api__InstanceCacheTtlSeconds",
      "label": "Server detail cache",
      "description": "How long a server's detail is reused before being re-read from the engine.",
      "group": "polling",
      "type": "int",
      "default": "60",
      "min": 10,
      "unit": "s",
      "risk": "safe"
    },
    {
      "key": "blueprintCacheTtlSec",
      "env": "Api__BlueprintCacheTtlSeconds",
      "label": "Blueprint cache",
      "description": "How long the blueprint catalog is reused before being re-read from the engine.",
      "group": "polling",
      "type": "int",
      "default": "60",
      "min": 10,
      "unit": "s",
      "risk": "safe"
    },
    {
      "key": "authDisabled",
      "env": "Api__AuthDisabled",
      "label": "Disable authentication",
      "description": "Turns off every authentication check and treats each caller as an administrator. Intended for local development only — on a reachable host it hands full control of every server to anyone who can connect.",
      "group": "auth",
      "type": "bool",
      "default": "false",
      "risk": "destructive"
    },
    {
      "key": "disabledAuthActor",
      "env": "Api__DisabledAuthActor",
      "label": "Actor while auth is disabled",
      "description": "Who the audit log names for work done while authentication is disabled, written provider:name (for example local:claude). Required whenever authentication is off: without it the host refuses to start, because every action taken through an open door would otherwise land in the record under a name nobody chose.",
      "group": "auth",
      "type": "string",
      "risk": "safe"
    },
    {
      "key": "usersDbPath",
      "env": "Api__UsersDbPath",
      "label": "Account replica",
      "description": "The file this node keeps its copy of the cluster's accounts in, kept current by the auth anchor. Every KGSM service on the machine reads the same file, so pointing this somewhere else resolves people against a copy nothing updates.",
      "group": "auth",
      "type": "path",
      "default": "/var/lib/kgsm/auth/users.db",
      "risk": "wiring"
    },
    {
      "key": "authorityCacheSeconds",
      "env": "Api__AuthorityCacheSeconds",
      "label": "Authority lookup cache",
      "description": "How long someone's authority is reused before it is looked up again. This is how long after an admin changes what a person may do that their next request can still go through at the old level.",
      "group": "auth",
      "type": "int",
      "default": "5",
      "min": 0,
      "unit": "s",
      "risk": "safe"
    },
    {
      "key": "sessionsCacheTtlMs",
      "env": "Api__SessionsCacheTtlMs",
      "label": "Session lookup cache",
      "description": "How long this node reuses its answer to whether a session has been ended. It is how long a session somebody has just signed out of can still be used here — longer means fewer lookups and a longer wait before a sign-out takes effect on this node.",
      "group": "auth",
      "type": "int",
      "default": "5000",
      "min": 500,
      "unit": "ms",
      "risk": "safe"
    },
    {
      "key": "localAnchorUrl",
      "env": "Api__LocalAnchorUrl",
      "label": "Local auth anchor",
      "description": "The address of the auth anchor on this machine. A node that knows no other member introduces itself here, which is how a machine that is its own cluster comes to have somebody who can sign people in. Once the node has any member at all it never asks again. Empty turns this off, for a node that only ever joins a cluster someone else runs.",
      "group": "auth",
      "type": "string",
      "default": "http://127.0.0.1:8098",
      "risk": "wiring"
    },
    {
      "key": "hostProviderFilePath",
      "env": "Api__HostProviderFilePath",
      "label": "Sign-in record for this machine",
      "description": "The file this node keeps saying who signs the cluster's sessions — the issuer and its keys — for services on this machine that are not members themselves, such as an assistant running beside this node. They accept only what this file names, so pointing it somewhere else leaves them accepting nobody.",
      "group": "auth",
      "type": "path",
      "default": "/var/lib/kgsm/cluster/auth-provider.json",
      "risk": "wiring"
    },
    {
      "key": "rawgApiKey",
      "env": "Api__RawgApiKey",
      "label": "RAWG API key",
      "description": "Key for the game-metadata service used for descriptions and artwork. Empty means the panel shows what the blueprints carry and nothing more.",
      "group": "library",
      "type": "secret",
      "risk": "safe"
    },
    {
      "key": "rawgCacheDir",
      "env": "Api__RawgCacheDir",
      "label": "Artwork cache directory",
      "description": "Where downloaded artwork is kept. Pointing it elsewhere abandons what is already downloaded and fetches everything again.",
      "group": "library",
      "type": "path",
      "risk": "destructive"
    },
    {
      "key": "steamCdnBase",
      "env": "Api__SteamCdnBaseUrl",
      "label": "Steam art base URL",
      "description": "Where Steam cover art is fetched from.",
      "group": "library",
      "type": "string",
      "default": "https://shared.fastly.steamstatic.com/store_item_assets/steam/apps",
      "risk": "safe"
    },
    {
      "key": "steamCoversDisabled",
      "env": "Api__SteamCoversDisabled",
      "label": "Disable Steam cover art",
      "description": "Stops using Steam for cover art, leaving the metadata service as the only source.",
      "group": "library",
      "type": "bool",
      "default": "false",
      "risk": "safe"
    },
    {
      "key": "libraryRefreshIntervalDays",
      "env": "Api__LibraryRefreshIntervalDays",
      "label": "Library refresh interval",
      "description": "How often the game library's metadata and artwork are refreshed. Zero turns the scheduled refresh off.",
      "group": "library",
      "type": "int",
      "default": "7",
      "min": 0,
      "unit": "days",
      "risk": "safe"
    },
    {
      "key": "libraryRefreshHour",
      "env": "Api__LibraryRefreshHour",
      "label": "Library refresh hour",
      "description": "Hour of the day the refresh runs, in this host's local time.",
      "group": "library",
      "type": "int",
      "default": "6",
      "min": 0,
      "max": 23,
      "risk": "safe",
      "dependsOn": "libraryRefreshIntervalDays"
    },
    {
      "key": "filesMaxEntries",
      "env": "Api__FilesMaxEntries",
      "label": "Directory listing limit",
      "description": "How many entries one directory listing returns before it is cut short.",
      "group": "files",
      "type": "int",
      "default": "200",
      "min": 1,
      "risk": "safe"
    },
    {
      "key": "filesMaxEditBytes",
      "env": "Api__FilesMaxEditBytes",
      "label": "Editable file size limit",
      "description": "How large a server file may be and still be opened in the editor.",
      "group": "files",
      "type": "int",
      "default": "2097152",
      "min": 1024,
      "unit": "bytes",
      "risk": "safe"
    },
    {
      "key": "blueprintMaxEditBytes",
      "env": "Api__BlueprintMaxEditBytes",
      "label": "Editable blueprint size limit",
      "description": "How large a blueprint may be and still be opened in the editor.",
      "group": "files",
      "type": "int",
      "default": "262144",
      "min": 1024,
      "unit": "bytes",
      "risk": "safe"
    },
    {
      "key": "logSources",
      "env": "Api__LogSources",
      "label": "Log sources",
      "description": "Which journal units the host's Logs tab reads. Empty uses the known leaf units.",
      "group": "logs",
      "type": "csv",
      "risk": "safe"
    },
    {
      "key": "logReadTimeoutMs",
      "env": "Api__LogReadTimeoutMs",
      "label": "Log read timeout",
      "description": "How long to wait for the journal before giving up on a log request.",
      "group": "logs",
      "type": "int",
      "default": "5000",
      "min": 500,
      "unit": "ms",
      "risk": "safe"
    },
    {
      "key": "leafOverridesDir",
      "env": "Api__LeafOverridesDir",
      "label": "Override directory",
      "description": "Where this API writes each leaf's configuration override. It has to match what the leaves' drop-ins load, or every change is written and never read.",
      "group": "leafconfig",
      "type": "path",
      "default": "/var/lib/kgsm-api/leaf-overrides",
      "risk": "wiring"
    },
    {
      "key": "leafDescriptorDir",
      "env": "Api__LeafDescriptorDir",
      "label": "Descriptor directory",
      "description": "Where leaves publish what they can be configured with. Pointing it elsewhere leaves every leaf's configuration page empty.",
      "group": "leafconfig",
      "type": "path",
      "default": "/var/lib/kgsm/leaves",
      "risk": "wiring"
    },
    {
      "key": "anchorDescriptorDir",
      "env": "Api__AnchorDescriptorDir",
      "label": "Anchor descriptor directory",
      "description": "Where an anchor sharing this machine publishes what it is. Read for one thing: a component described here is a peer of this node rather than one of its services, so it is left off the services board. Pointing it elsewhere makes this node claim the anchors it runs beside.",
      "group": "leafconfig",
      "type": "path",
      "default": "/var/lib/kgsm/anchors",
      "risk": "wiring"
    },
    {
      "key": "leafDropInDir",
      "env": "Api__LeafDropInDir",
      "label": "Drop-in directory",
      "description": "One directory to look for unit files and their drop-ins in, instead of everywhere systemd looks. Empty searches all of them, which is what finds a leaf's unit whether its package installed it or a deploy script did.",
      "group": "leafconfig",
      "type": "path",
      "risk": "wiring"
    },
    {
      "key": "leafApplyCanaryMs",
      "env": "Api__LeafApplyCanaryMs",
      "label": "Post-change health window",
      "description": "How long a leaf is watched after a configuration change before the change is rolled back as unhealthy.",
      "group": "leafconfig",
      "type": "int",
      "default": "15000",
      "min": 2000,
      "unit": "ms",
      "risk": "safe"
    },
    {
      "key": "clusterGossipUrl",
      "env": "Api__ClusterGossipUrl",
      "label": "Peer URL",
      "description": "An address only other hosts use, when they must not go the way a browser does. Empty means hosts and browsers reach this one the same way.",
      "group": "cluster",
      "type": "string",
      "risk": "wiring"
    },
    {
      "key": "clusterGossipMs",
      "env": "Api__ClusterGossipMs",
      "label": "Peer exchange interval",
      "description": "How often this host exchanges what it knows with a peer.",
      "group": "cluster",
      "type": "int",
      "default": "5000",
      "min": 250,
      "unit": "ms",
      "risk": "safe"
    },
    {
      "key": "clusterPollMs",
      "env": "Api__ClusterPollMs",
      "label": "Peer refresh interval",
      "description": "How often each peer's state is re-read.",
      "group": "cluster",
      "type": "int",
      "default": "10000",
      "min": 250,
      "unit": "ms",
      "risk": "safe"
    },
    {
      "key": "clusterSuspectMs",
      "env": "Api__ClusterSuspectMs",
      "label": "Peer suspect timeout",
      "description": "How long a silent peer is tolerated before it is treated as suspect.",
      "group": "cluster",
      "type": "int",
      "default": "30000",
      "min": 1000,
      "unit": "ms",
      "risk": "safe"
    },
    {
      "key": "clusterReapMs",
      "env": "Api__ClusterReapMs",
      "label": "Peer removal timeout",
      "description": "How long a suspect peer is kept before it is dropped from the cluster.",
      "group": "cluster",
      "type": "int",
      "default": "300000",
      "min": 1000,
      "unit": "ms",
      "risk": "safe"
    },
    {
      "key": "clusterDrainMs",
      "env": "Api__ClusterDrainMs",
      "label": "Outbox drain interval",
      "description": "How often queued messages for other hosts are sent.",
      "group": "cluster",
      "type": "int",
      "default": "1000",
      "min": 250,
      "unit": "ms",
      "risk": "safe"
    },
    {
      "key": "clusterGcMs",
      "env": "Api__ClusterGcMs",
      "label": "Cluster cleanup interval",
      "description": "How often delivered and expired cluster messages are cleared out.",
      "group": "cluster",
      "type": "int",
      "default": "600000",
      "min": 60000,
      "unit": "ms",
      "risk": "safe"
    },
    {
      "key": "clusterRetryTtlDays",
      "env": "Api__ClusterRetryTtlDays",
      "label": "Undelivered message lifetime",
      "description": "How long a message for an unreachable host keeps being retried before it is given up on.",
      "group": "cluster",
      "type": "int",
      "default": "7",
      "min": 1,
      "unit": "days",
      "risk": "safe"
    },
    {
      "key": "clusterRetentionDays",
      "env": "Api__ClusterRetentionDays",
      "label": "Cluster message retention",
      "description": "How long delivered cluster messages are kept before being deleted. Must outlast the retry window, or a message could be deleted while still being retried.",
      "group": "cluster",
      "type": "int",
      "default": "30",
      "min": 2,
      "unit": "days",
      "risk": "destructive"
    },
    {
      "key": "dbPath",
      "env": "Api__DbPath",
      "label": "Database file",
      "description": "File holding this API's own records — the audit trail, live sessions, and per-host settings. Pointing it elsewhere starts empty and leaves the existing history behind.",
      "group": "storage",
      "type": "path",
      "default": "kgsm-api.db",
      "risk": "destructive"
    },
    {
      "key": "logLevel",
      "env": "Logging__LogLevel__Default",
      "label": "Log level",
      "description": "Minimum severity this API logs.",
      "group": "general",
      "type": "enum",
      "values": [
        "Trace",
        "Debug",
        "Information",
        "Warning",
        "Error",
        "Critical"
      ],
      "default": "Information",
      "risk": "safe"
    },
    {
      "key": "bindAddress",
      "env": "Api__Urls",
      "label": "Listen address",
      "description": "Addresses this API serves on. The Control Panel and every other surface reach it here.",
      "group": "general",
      "type": "string",
      "default": "http://0.0.0.0:8080",
      "risk": "wiring"
    },
    {
      "key": "corsOrigins",
      "env": "Api__CorsOrigins",
      "label": "Allowed browser origins",
      "description": "Exact origins a browser may call this API from. A panel served from an origin that is not listed is refused by the browser before the request arrives.",
      "group": "general",
      "type": "csv",
      "risk": "wiring"
    }
  ]
}
